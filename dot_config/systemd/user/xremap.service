[Unit]
Description=xremap
After=niri.service bluetooth.service
Requires=niri.service

[Service]
StandardOutput=journal
StandardError=journal
Restart=always
; Environment=RUST_LOG=DEBUG
ExecStart=xremap --watch .config/xremap/config.yml --device 'USB Keyboard','Logitech','Wii','Corne','AT Translated Set 2 keyboard' --ignore 'USB Keyboard System Control','USB Keyboard Consumer Control'

[Install]
WantedBy=graphical-session.target
