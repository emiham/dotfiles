{
  "Chezmoi Hostname": {
    "prefix": "hostname",
    "body": [
      "{{- if eq .chezmoi.hostname \"${1:hostname}\" }}",
      "$0",
      "{{- end }}"
    ],
    "description": "Chezmoi hostname template conditional"
  }
}
